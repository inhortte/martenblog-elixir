defmodule Martenblog.Router do
  defmacro __using__(_opts) do
    quote do
      def init(options) do
	Logger.info "Martenblog emerges once again from the slime"
	options
      end
      def call(conn, _opts) do
	route(conn.method, conn.path_info, conn)
      end
    end
  end
end
